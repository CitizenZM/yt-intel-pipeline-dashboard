{
  "name": "yt-intel-pipeline-dashboard",
  "version": "1.0.0",
  "description": "YouTube Intelligence Pipeline Dashboard — 300 AI/startup channel monitor",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "python3 -m http.server 8780 --bind 127.0.0.1 --directory public"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
